import 'package:flutter/material.dart';

class OnboardingCountry extends StatelessWidget {
  const OnboardingCountry({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
